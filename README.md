# repo-name

A minimal Python template with lean defaults for future projects.

## Working Defaults

- Use red/green TDD
- Run tests with `uv run pytest -q`
- Use `uv run ...` to execute project commands
- Write google docstrings
